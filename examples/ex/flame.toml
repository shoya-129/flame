[package]
name = "flame_examples"
version = "0.1.0"
edition = "2026"
type = "executable"

[dependencies]

[native-dependencies]
uuid = { version = "*", features = ["v4"] }

[plugins]
server = "./native"
