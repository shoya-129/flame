---
title: Flame Programming Language
description: A fast, safe, and modern systems language powered by it's Blaze compiler that natively compiles Rust code.
template: splash
hero:
  tagline: A fast, statically typed systems programming language with zero-cost Rust interop, native test runner, memory safety without GC, and tailored runtimes.
  actions:
    - text: Get Started
      link: /getting-started/introduction/
      icon: right-arrow
      variant: primary
    - text: Explore Standard Library
      link: /std/overview/
      icon: open-book
      variant: secondary
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';
import LucideCard from '../../components/LucideCard.astro';
import { Trash2, Puzzle, Cpu, CheckCircle2, ShieldCheck, Box, Zap, Server } from 'lucide-astro';

## Why Flame?

<CardGrid>
	<LucideCard title="Zero-Overhead Rust Interoperability">
		<Puzzle slot="icon" />
		Import any Rust crate directly in `flame.toml` with `import native.uuid` or `import native.axum`. Statically linked with zero FFI overhead.
	</LucideCard>
	<LucideCard title="No Garbage Collector, Pure Safety">
		<Trash2 slot="icon" />
		Rust-style Ownership and Borrowing system guarantees memory safety and data-race prevention at compile time with zero GC pauses.
	</LucideCard>
	<LucideCard title="True Multithreaded Concurrency">
		<Cpu slot="icon" />
		Combines **Tokio worker pools** for non-blocking I/O with dedicated **`thread { ... }`** compute workers across multi-core CPUs.
	</LucideCard>
	<LucideCard title="Integrated Native Test Runner">
		<CheckCircle2 slot="icon" />
		PascalCase annotations like `@Test`, `@Setup`, `@Cleanup`, and `@Parameterized` with zero-cost production stripping during release builds.
	</LucideCard>
</CardGrid>

---

## Flame in Production & Tailored Architecture

Unlike traditional languages that require massive universal runtimes with unused standard libraries and heavy interpreters, **Flame generates a specialized native runtime for each application**. Blaze analyzes imported modules and `.fmi` interface contracts, emitting an optimized, purpose-built native binary via Rust, Cargo, and LLVM.

<CardGrid>
	<LucideCard title="No Rust in Production">
		<ShieldCheck slot="icon" />
		The runner container does not need `rustc`, `cargo`, or `.rs` source files. All native plugins and dependencies are statically baked into the executable.
	</LucideCard>
	<LucideCard title="Minimal Image Size (<80MB)">
		<Box slot="icon" />
		By excluding compilation toolchains and dead standard library subsystems, production container images typically stay under **80MB**.
	</LucideCard>
	<LucideCard title="Instant Boot Time">
		<Zap slot="icon" />
		Specialized native runtimes start in milliseconds without JVM warmup, V8 cold-start overhead, or universal interpreter startup lag.
	</LucideCard>
	<LucideCard title="Hardened Security">
		<Server slot="icon" />
		Radically minimized attack surface with zero package managers, build compilers, or unreferenced native code exposed in production.
	</LucideCard>
</CardGrid>

### Dive Deeper into Flame

<CardGrid>
	<LinkCard
		title="Architecture & Compiler Deep-Dive"
		description="Learn how the Blaze compilation pipeline specializes runtimes, compiles .fmi interfaces, and maintains zero GC overhead."
		href="/getting-started/architecture/"
	/>
	<LinkCard
		title="Production & Docker Deployment Guide"
		description="Explore multi-stage Docker builds, lean distroless runner setups, and production cloud orchestration with Flame."
		href="/getting-started/deployment-and-docker/"
	/>
</CardGrid>
