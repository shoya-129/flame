package hardware

@Docs("Returns system CPU temperature sensors where available.")
export fn cpuTemp() -> Float {}

@Docs("Returns CPU load percentage across logical cores.")
export fn cpuUsage() -> Float {}

@Docs("Returns battery charge percentage (0-100) or nil if not running on battery.")
export fn batteryPercent() -> Float {}
