package hid

@Docs("Enumerates USB HID devices attached to the host.")
export fn devices() -> Vec<Formula> {}

@Docs("Opens a connection to a specific HID device by vendor and product ID.")
export fn open(vendor_id: Int, product_id: Int) -> Any {}
