[package]
name = "automation"
version = "0.1.0"
edition = "2026"
type = "executable"

[dependencies]
std = "0.1.0"

[native-dependencies]
uuid = { version = "*", features = ["v4"] }
rand = "0.10.2"

[plugins]
bridge = "./native"
